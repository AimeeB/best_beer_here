%section.show-page
  %h2 Breweries
  #map_canvas.float-left(style="width:100%; height:100%")

  %ul.breweries
    - @breweries.each do |brewery|
      %li
        = link_to brewery.name, brewery_path(brewery)